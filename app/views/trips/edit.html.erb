<div class="row">
  <div class="col s12">
    <h4 class="right-align"> Edit Trip Info: <%= @trip.name %> </h4>
  </div>
  <div class="col s12 right-align"> 
    <i class="material-icons">delete_forever</i><%= link_to 'Delete', trip_path(@trip),
    method: :delete, data: {confirm: 'You will lose everything in this trip, are you sure you want to delete?'} %>
  </div>
  <%= render(partial:'form') %>

  <%=link_to 'BACK', trips_path %>

</div>
